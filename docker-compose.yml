services:
  app_bingo:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: app_bingo
    restart: unless-stopped
    ports:
      - "8082:8082"
    environment:
      - NODE_ENV=production
      - PORT=8082
